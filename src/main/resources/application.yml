spring:
  datasource:
    url: jdbc:mysql://dev-ttokttok.cjyuygmyir9b.ap-northeast-2.rds.amazonaws.com:3306/ttokttok
    username: root
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
        hbm2ddl:
          auto: update
        default_batch_fetch_size: 1000


  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SMTP_EMAIL}
    password: ${SMTP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: "VlwEyVBsYt9V7zq57TejMnVUyzblYcfPQye08f7MGVA9XkHa"
  access-token-validity-in-seconds: 3600
  refresh-token-validity-in-seconds: 86400
  
  
  ai:
    vertex:
      ai:
        gemini:
          # 1. (필수) Google Cloud 프로젝트 ID
          project-id: "gen-lang-client-0133965444"
          # 2. (필수) Vertex AI 리전 (예: us-central1)
          location: "us-central1"
          chat:
            options:
              # 3. 모델명 (요청하신 2.5 Flash)
              model: "gemini-2.5-flash-latest"
              temperature: 0.4
