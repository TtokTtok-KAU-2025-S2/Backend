package TtokTtok.Backend.repository;

import TtokTtok.Backend.domain.knock.KnockResult;
import org.springframework.data.jpa.repository.JpaRepository;

public interface KnockResultRepository extends JpaRepository<KnockResult, Long> {
}
